--[[
  Knetik Platform API Documentation latest 
 
  This is the spec for the Knetik API.  Use this in conjunction with the documentation found at https://knetikcloud.com.
 
  OpenAPI spec version: latest 
  Contact: support@knetik.com
  Generated by: https://github.com/swagger-api/swagger-codegen.git
]]

-- subscription_plan_resource class
local subscription_plan_resource = {}
local subscription_plan_resource_mt = {
	__name = "subscription_plan_resource";
	__index = subscription_plan_resource;
}

local function cast_subscription_plan_resource(t)
	return setmetatable(t, subscription_plan_resource_mt)
end

local function new_subscription_plan_resource(additional_properties, billing_cycle_length, billing_cycle_unit, consolidated, currency_code, end_date, first_billing_cycle_length, first_billing_cycle_unit, grace_period, id, initial_fee, initial_sku, late_payment_fee, late_payment_sku, locked, max_bill_attempts, max_cycles, migrate_to_plan, min_cycles, name, published, reactivation_fee, reactivation_sku, recurring_fee, recurring_sku, start_date)
	return cast_subscription_plan_resource({
		["additional_properties"] = additional_properties;
		["billing_cycle_length"] = billing_cycle_length;
		["billing_cycle_unit"] = billing_cycle_unit;
		["consolidated"] = consolidated;
		["currency_code"] = currency_code;
		["end_date"] = end_date;
		["first_billing_cycle_length"] = first_billing_cycle_length;
		["first_billing_cycle_unit"] = first_billing_cycle_unit;
		["grace_period"] = grace_period;
		["id"] = id;
		["initial_fee"] = initial_fee;
		["initial_sku"] = initial_sku;
		["late_payment_fee"] = late_payment_fee;
		["late_payment_sku"] = late_payment_sku;
		["locked"] = locked;
		["max_bill_attempts"] = max_bill_attempts;
		["max_cycles"] = max_cycles;
		["migrate_to_plan"] = migrate_to_plan;
		["min_cycles"] = min_cycles;
		["name"] = name;
		["published"] = published;
		["reactivation_fee"] = reactivation_fee;
		["reactivation_sku"] = reactivation_sku;
		["recurring_fee"] = recurring_fee;
		["recurring_sku"] = recurring_sku;
		["start_date"] = start_date;
	})
end

return {
	cast = cast_subscription_plan_resource;
	new = new_subscription_plan_resource;
}
