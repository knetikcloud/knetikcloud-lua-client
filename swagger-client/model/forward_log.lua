--[[
  Knetik Platform API Documentation latest 
 
  This is the spec for the Knetik API.  Use this in conjunction with the documentation found at https://knetikcloud.com.
 
  OpenAPI spec version: latest 
  Contact: support@knetik.com
  Generated by: https://github.com/swagger-api/swagger-codegen.git
]]

-- forward_log class
local forward_log = {}
local forward_log_mt = {
	__name = "forward_log";
	__index = forward_log;
}

local function cast_forward_log(t)
	return setmetatable(t, forward_log_mt)
end

local function new_forward_log(end_date, error_msg, event_id, headers, http_status_code, id, method, payload, response, retry_count, retryable, rule_id, start_date, success, url)
	return cast_forward_log({
		["end_date"] = end_date;
		["error_msg"] = error_msg;
		["event_id"] = event_id;
		["headers"] = headers;
		["http_status_code"] = http_status_code;
		["id"] = id;
		["method"] = method;
		["payload"] = payload;
		["response"] = response;
		["retry_count"] = retry_count;
		["retryable"] = retryable;
		["rule_id"] = rule_id;
		["start_date"] = start_date;
		["success"] = success;
		["url"] = url;
	})
end

return {
	cast = cast_forward_log;
	new = new_forward_log;
}
